# frozen_string_literal: true

source 'https://rubygems.org'

ruby '2.7.4'

# RSpec is used for writing and running test cases
gem 'rspec'

# RuboCop for linting and code style checks
gem 'rubocop', require: false
